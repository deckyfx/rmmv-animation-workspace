# Local Development Configuration
# This file is used when running locally with bun run dev
# For Docker Compose, use the root .env file instead

# Server Port (local development)
# The port the application listens on when running locally
SERVER_PORT=10021
